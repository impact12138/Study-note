repeat()方法返回一个新字符串，表示将原字符串重复n次，参数为n
当参数为Infinity或负数时会报错

padStart()和padEnd()//分别用于头部和尾部的字符串补全长度功能
一共接受两个参数，第一个参数用来指定字符串的最小长度，第二个
参数是用来补全的字符串

matchAll()方法返回一个正则表达式在当前字符串的所有匹配

新的字符串拼接方法:`<b>${}</b>`
模板字符串中嵌入变量，需要将变量名写在${}之中

模板使用<%= ... %>输出 JavaScript 表达式