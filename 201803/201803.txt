MVVM框架--Vue.js,React.js,Angular.js

页面DOM    监控者         数据
View      ViewModel       Model
页面是我们所看到的页面视图，Model相当于我们的数据
对象，而中间的监控者负责监控两侧的数据，并相对应地
通知另一侧 进行修改。
Vue.js:通过尽可能简单的API实现响应的数据绑定和组合
的视图组件
Vue的数据驱动:只需改变数据，就会自动改变视图；
视图组件化:把整一个网页拆分成一个个区块，每个区块
看作一个组件，网页由多个组件拼接或者嵌套组成；
//IE8及以下版本浏览器不兼容